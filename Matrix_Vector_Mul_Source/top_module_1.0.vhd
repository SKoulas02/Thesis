 library library IEEE;
 use IEEE.std_logic_1164.all;
 use IEEE.numeric_std.all;
 
 entity top_module is
    generic(

    );

    port(
        
    );


 end entity top_module;
 -- type StateMachine is (ResetState, WaitValid, Busy, RefreshCi);
    -- signal State : StateMachine := ResetState;
    -- State <= ResetState;